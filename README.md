# Mandatory-Activity-4
Implementation of Ricart-Argawala

How to run:

In 3 seperate terminals:

Node 1 - Server:

```
go run ./server/node --node=1 --total=3
```

Node 2 - Client + Server:

```
go run ./server/node --node=2 --total=3
```

Node 3 - Client + Server:

```
go run ./server/node --node=3 --total=3
```
